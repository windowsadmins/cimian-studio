<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CimianStudio.Views.RepositoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Page.Resources>
        <DataTemplate x:Key="RecentRowTemplate" x:DataType="x:String">
            <TextBlock Text="{x:Bind}" TextTrimming="CharacterEllipsis" Padding="4,2,4,2" />
        </DataTemplate>
    </Page.Resources>

    <ScrollViewer>
        <Grid Padding="32" RowSpacing="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Spacing="4">
                <TextBlock
                    x:Name="RepoNameText"
                    Style="{StaticResource TitleLargeTextBlockStyle}" />
                <TextBlock
                    x:Name="RepoPathText"
                    Style="{StaticResource BodyTextBlockStyle}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    TextWrapping="Wrap" />
            </StackPanel>

            <Grid Grid.Row="1" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0"
                         x:Name="SearchBox"
                         PlaceholderText="Search packages and manifests content"
                         TextChanged="OnSearchTextChanged" />
                <Border Grid.Column="1"
                        x:Name="IndexingPill"
                        Visibility="Collapsed"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="10,4,10,4"
                        VerticalAlignment="Center">
                    <TextBlock x:Name="IndexingPillText"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </Border>
            </Grid>

            <Border Grid.Row="2"
                    x:Name="SearchResultsCard"
                    Visibility="Collapsed"
                    Style="{StaticResource CardStyle}" Shadow="{StaticResource CardShadow}" Translation="0,0,16"
                    Padding="20,12,20,12">
                <StackPanel Spacing="8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   x:Name="SearchResultsHeader"
                                   Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock Grid.Column="1"
                                   x:Name="SearchResultsCount"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   VerticalAlignment="Center" />
                    </Grid>
                    <ListView x:Name="SearchResultsList"
                              SelectionMode="None"
                              IsItemClickEnabled="True"
                              ItemClick="OnSearchResultClicked"
                              MaxHeight="360" />
                </StackPanel>
            </Border>

            <ItemsControl
                Grid.Row="3"
                x:Name="StatsHost">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" Spacing="16" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>

            <Grid Grid.Row="4" ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Style="{StaticResource CardStyle}" Shadow="{StaticResource CardShadow}" Translation="0,0,16"
                        Padding="20,16,20,12">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Recently modified pkginfo"
                                   Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <ListView x:Name="RecentPackagesList"
                                  SelectionMode="None"
                                  IsItemClickEnabled="True"
                                  ItemClick="OnRecentPackageClicked"
                                  MaxHeight="220"
                                  ItemTemplate="{StaticResource RecentRowTemplate}" />
                        <TextBlock x:Name="NoRecentPackagesText"
                                   Text="No recently modified packages."
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                   Visibility="Collapsed" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="1"
                        Style="{StaticResource CardStyle}" Shadow="{StaticResource CardShadow}" Translation="0,0,16"
                        Padding="20,16,20,12">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Recently modified manifests"
                                   Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <ListView x:Name="RecentManifestsList"
                                  SelectionMode="None"
                                  IsItemClickEnabled="True"
                                  ItemClick="OnRecentManifestClicked"
                                  MaxHeight="220"
                                  ItemTemplate="{StaticResource RecentRowTemplate}" />
                        <TextBlock x:Name="NoRecentManifestsText"
                                   Text="No recently modified manifests."
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                   Visibility="Collapsed" />
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Row="5"
                    x:Name="GitStatusCard"
                    Visibility="Collapsed"
                    Style="{StaticResource CardStyle}" Shadow="{StaticResource CardShadow}" Translation="0,0,16"
                    Padding="20,12,12,12">
                <Grid ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Spacing="2" VerticalAlignment="Center">
                        <TextBlock x:Name="GitStatusTitle"
                                   Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock x:Name="GitStatusScope"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                    <HyperlinkButton Grid.Column="1"
                                     x:Name="OpenGitTabButton"
                                     Content="Open Git tab"
                                     Click="OnOpenGitTabClicked" />
                </Grid>
            </Border>

            <Button
                Grid.Row="6"
                Content="Validate repository"
                Click="OnValidateClicked"
                HorizontalAlignment="Left" />

            <InfoBar
                Grid.Row="7"
                x:Name="ValidationBar"
                IsClosable="False"
                IsOpen="False" />

            <ScrollViewer Grid.Row="8" MaxHeight="240">
                <ItemsControl x:Name="ValidationDetails">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <TextBlock Text="{x:Bind}" TextWrapping="Wrap" Margin="0,2,0,2" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Repository insights: orphans + top facets + largest packages. Computed
                 once per Loaded; refreshes when packages or manifests change. Each row
                 click navigates to the relevant page with that selection. -->
            <Border Grid.Row="9"
                    x:Name="InsightsCard"
                    Style="{StaticResource CardStyle}"
                    Shadow="{StaticResource CardShadow}"
                    Translation="0,0,16"
                    Padding="20,16,20,16">
                <StackPanel Spacing="12">
                    <TextBlock Text="Repository insights"
                               Style="{StaticResource BodyStrongTextBlockStyle}" />
                    <Grid ColumnSpacing="20" RowSpacing="20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Spacing="6">
                            <TextBlock Text="Orphan packages" Style="{StaticResource BodyStrongTextBlockStyle}" />
                            <TextBlock x:Name="OrphansHint"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       Text="Packages not referenced by any manifest." />
                            <ListView x:Name="OrphansList"
                                      SelectionMode="None"
                                      IsItemClickEnabled="True"
                                      ItemClick="OnOrphanClicked"
                                      MaxHeight="180" />
                            <TextBlock x:Name="OrphansEmpty"
                                       Text="None — every package is referenced by a manifest."
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                       Visibility="Collapsed" />
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Spacing="6">
                            <TextBlock Text="Largest packages" Style="{StaticResource BodyStrongTextBlockStyle}" />
                            <TextBlock Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       Text="Top 5 by installer size." />
                            <ListView x:Name="LargestList"
                                      SelectionMode="None"
                                      IsItemClickEnabled="True"
                                      ItemClick="OnLargestClicked"
                                      MaxHeight="180" />
                            <TextBlock x:Name="LargestEmpty"
                                       Text="No installer sizes recorded yet."
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                       Visibility="Collapsed" />
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="0" Spacing="6">
                            <TextBlock Text="Top categories" Style="{StaticResource BodyStrongTextBlockStyle}" />
                            <ListView x:Name="TopCategoriesList"
                                      SelectionMode="None"
                                      MaxHeight="180" />
                            <TextBlock x:Name="TopCategoriesEmpty"
                                       Text="No categories yet."
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                       Visibility="Collapsed" />
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="1" Spacing="6">
                            <TextBlock Text="Top developers" Style="{StaticResource BodyStrongTextBlockStyle}" />
                            <ListView x:Name="TopDevelopersList"
                                      SelectionMode="None"
                                      MaxHeight="180" />
                            <TextBlock x:Name="TopDevelopersEmpty"
                                       Text="No developers yet."
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                       Visibility="Collapsed" />
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
